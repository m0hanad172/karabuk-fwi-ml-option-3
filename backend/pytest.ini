[pytest]
# Make `src.*` and `configs.*` importable from anywhere — equivalent to the
# `sys.path.insert` calls already in tests/conftest.py, but also makes
# `python -m pytest backend/tests` work from the repo root.
pythonpath = .
testpaths = tests
addopts = -ra -p no:cacheprovider
